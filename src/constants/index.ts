import path from "path";

export const notificationsFilePath = path.resolve(
  __dirname,
  "../data/notifications.json"
);
export const ordersFilePath = path.resolve(__dirname, "../data/orders.json");
export const itemsFilePath = path.resolve(__dirname, "../data/items.json");
